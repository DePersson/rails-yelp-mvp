<nav class="navbar navbar-dark bg-dark">
  <span class="navbar-brand mb-0 h1"><h1>Add a review</h1></span>
</nav>

<div class="container">
  <div class="card">
    <%= simple_form_for [@restaurant, @review] do |f| %>
      <%= f.input :content %>
      <%= f.input :rating %>
      <%= f.submit "Add a review", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
